[project]
name = "ibkr-vertex-ai-fund"
version = "0.1.0"
description = "Local Docker baseline for IBKR Client Portal Gateway experiments."
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.0",
]

[tool.uv]
package = false
